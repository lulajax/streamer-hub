server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: mca-server
  
  profiles:
    active: dev
  
  datasource:
    url: jdbc:postgresql://192.168.84.30:5432/mca_db
    username: mca_db
    password: myxzt63MF6Msej6A
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false
  
  redis:
    host: 192.168.84.30
    port: 6379
    password: redis_aYWNrS
    database: 1
    timeout: 2000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
  
  jackson:
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false
  
  # Mail Configuration
  mail:
    host: smtp.exmail.qq.com
    port: 587
    username: junjie.lu@scienjoy.com
    password: A201DE4731D83E07AA7892B402BACD71CB5DF595E613D9A8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Logging
logging:
  level:
    com.mca.server: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# JWT Configuration
jwt:
  secret: mca-secret-key-for-jwt-signing-must-be-at-least-256-bits-long
  expiration: 86400000  # 24 hours
  refresh-expiration: 604800000  # 7 days

# CORS Configuration
cors:
  allowed-origins: "*"
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  max-age: 3600

# MCA Application Settings
mca:
  auth:
    max-devices: 2
  activation:
    code-length: 16
    default-expiry-days: 365
  gift:
    decay-check-interval: 5000  # 5 seconds
    auto-save-interval: 30000   # 30 seconds
  widget:
    base-url: http://192.168.84.30:3000
  app:
    frontend-url: http://192.168.84.30:5173
  tiktok:
    sign-api-key: ""  # Optional: Euler Stream API key for higher rate limits
